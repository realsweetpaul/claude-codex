# XLI Release — build all platforms on GitHub Actions, publish to Artifactory.
#
# Trigger: push a tag like `rust-v0.2.0` or manual dispatch.
# Builds macOS arm64 + Linux x86_64 (glibc 2.39) + Linux EL9 (glibc 2.34).
#
# Usage:
#   git tag rust-v0.2.0 && git push origin rust-v0.2.0
#   # or: Actions tab → xli-release → Run workflow
name: xli-release

on:
  push:
    tags:
      - "rust-v*.*.*"
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (e.g. 0.2.0) — reads from Cargo.toml if empty'
        required: false
        type: string

concurrency:
  group: xli-release
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  ARTIFACTORY_BASE: https://generic.repo.eng.netapp.com/user/palanisd

jobs:
  version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.ver.outputs.version }}
    steps:
      - uses: actions/checkout@v4
      - name: Determine version
        id: ver
        run: |
          if [[ -n "${{ inputs.version }}" ]]; then
            VER="${{ inputs.version }}"
          elif [[ "${GITHUB_REF_NAME}" =~ ^rust-v(.+)$ ]]; then
            VER="${BASH_REMATCH[1]}"
          else
            VER="$(grep '^version = ' codex-rs/Cargo.toml | head -1 | sed 's/version = "\(.*\)"/\1/')"
          fi
          echo "version=$VER" >> "$GITHUB_OUTPUT"
          echo "Building XLI v$VER"

  build:
    needs: version
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 60
    defaults:
      run:
        working-directory: codex-rs
    env:
      CARGO_PROFILE_RELEASE_LTO: thin
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: macos-15
            target: aarch64-apple-darwin
            artifact: xli-darwin-arm64
          - runner: ubuntu-24.04
            target: x86_64-unknown-linux-gnu
            artifact: xli-linux-amd64

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install Linux build deps
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update -y
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends pkg-config libcap-dev

      - name: Build
        run: |
          cargo build --target ${{ matrix.target }} --release --bin xli

      - name: Rename binary
        run: |
          cp target/${{ matrix.target }}/release/xli ${{ matrix.artifact }}
          chmod +x ${{ matrix.artifact }}
          ls -lh ${{ matrix.artifact }}

      - uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact }}
          path: codex-rs/${{ matrix.artifact }}

  # ── EL9-compatible build (glibc 2.34) ──────────────────────────────
  build-el9:
    needs: version
    name: Build linux-el9
    runs-on: ubuntu-24.04
    container: almalinux:9
    timeout-minutes: 60
    defaults:
      run:
        working-directory: codex-rs
    env:
      CARGO_PROFILE_RELEASE_LTO: thin

    steps:
      - uses: actions/checkout@v4

      - name: Install EL9 build deps + Rust
        working-directory: .
        run: |
          dnf install -y gcc gcc-c++ make pkg-config openssl-devel libcap-devel curl perl
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain stable
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"

      - name: Build
        run: |
          source "$HOME/.cargo/env"
          cargo build --target x86_64-unknown-linux-gnu --release --bin xli

      - name: Rename binary
        run: |
          cp target/x86_64-unknown-linux-gnu/release/xli xli-linux-el9
          chmod +x xli-linux-el9
          ls -lh xli-linux-el9
          # Show max glibc version for verification
          objdump -T xli-linux-el9 2>/dev/null | grep -oP 'GLIBC_\d+\.\d+' | sort -uV | tail -1 || true

      - uses: actions/upload-artifact@v7
        with:
          name: xli-linux-el9
          path: codex-rs/xli-linux-el9

  publish:
    needs: [version, build, build-el9]
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ needs.version.outputs.version }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Publish to Artifactory
        env:
          ARTIFACTORY_TOKEN: ${{ secrets.ARTIFACTORY_TOKEN }}
        run: |
          set -euo pipefail

          upload() {
            local file="$1" dest="$2"
            echo "▸ Uploading $(basename "$file") → $dest"
            curl -fsSL -X PUT \
              -H "Authorization: Bearer $ARTIFACTORY_TOKEN" \
              -T "$file" \
              "$dest"
            echo "  ✓ $(basename "$file")"
          }

          BASE="${ARTIFACTORY_BASE}/xli"

          # Binaries — versioned + release alias
          for artifact in xli-darwin-arm64 xli-linux-amd64 xli-linux-el9; do
            FILE="artifacts/${artifact}/${artifact}"
            chmod +x "$FILE"
            upload "$FILE" "${BASE}/${VERSION}/${artifact}"
            upload "$FILE" "${BASE}/release/${artifact}"
          done

          # install.sh
          upload "deploy/install.sh" "${BASE}/${VERSION}/install.sh"
          upload "deploy/install.sh" "${BASE}/release/install.sh"

          # version.txt
          echo "$VERSION" > /tmp/version.txt
          upload "/tmp/version.txt" "${BASE}/${VERSION}/version.txt"
          upload "/tmp/version.txt" "${BASE}/release/version.txt"

          echo ""
          echo "╔════════════════════════════════════════╗"
          echo "║   XLI v${VERSION} published            ║"
          echo "╚════════════════════════════════════════╝"
          echo ""
          echo "Install: curl -fsSL ${BASE}/release/install.sh | bash"
